generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Na razie nie potrzebujemy żadnych modeli w bazie danych dla MVP.
// Zostawiamy plik w tej formie jako punkt wyjścia do Fazy 2,
// kiedy dodamy model User dla subskrypcji.